{
  "name": "conservatory-conversions-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for Conservatory Conversions — partials stitched into public/ by build.js.",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch"
  }
}
