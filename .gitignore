# Build output — regenerated by `node build.js`
public/

# Design working material — JSX source from the approved design project, not deployed
design-source/

# Stage 4 raw source photos — inputs to the image pipeline; only the optimised
# assets/*.webp outputs are committed, not these originals
_stage4-src/

# Dependencies (none yet, but in case any are added later)
node_modules/

# Local environment / secrets — API keys live in the Vercel dashboard, never in git
.env
.env.*
.vercel/

# OS cruft
.DS_Store
**/.DS_Store
